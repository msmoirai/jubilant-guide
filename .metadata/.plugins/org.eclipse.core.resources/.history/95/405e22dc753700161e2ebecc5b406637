
public class TypesAndVariables {
	public static void main(String[] args) {
		int myVariable; //declaring a variable called myVariable that stores the type int, or integers
		myVariable = 5; //initializing myVariable to the value 5
		//can't view as mathematical - this is assigning value
		
		int myVar2=7; //declare and initialize both steps on same line
		//int is only intiger, not fractional value
		
		String hello = "Hello, "; //String must be capitalized, and value in quotes
		String name = "Charlie";
		
		System.out.println(hello);
		
		
	}
}
